[tool.poetry]
name = "lunarlink-backend"
version = "0.1.0"
description = ""
authors = [""]
readme = "README.md"

[tool.poetry.dependencies]
python = "3.9.x"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
